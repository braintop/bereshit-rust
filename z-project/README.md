# z-project

React + Vite frontend for login/register against the Rust API in `e5-mongo-refactor-bycrypt`.

## Run

```bash
npm install
npm run dev
```

API URL is set in `.env`:

```env
VITE_API_URL=http://127.0.0.1:8080
```
